from typing import Optional

from tables import Table

from lorelie.expressions import Q


class Index:
    prefix: str = ...
    max_name_length: int = ...
    index_name: str = ...
    name: str = ...
    fields: list[str] = ...
    index_name: str = ...
    condition: Q = ...

    def __init__(
        self,
        name: str,
        fields: Optional[list[str]] = ...,
        condition: Optional[Q] = ...
    ) -> None: ...

    def __repr__(self) -> str: ...
    def as_sql(self, table: Table) -> str: ...
